<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.weddingplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />
        <activity android:name=".NewPostActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".PostDetailActivity" />
        <activity android:name=".NewVendorCategoryActivity"></activity>
        <activity android:name=".VendorsCategoriesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MyUploadService"
            android:exported="false" />

        <activity
            android:name=".AddVendorActivity"
            android:windowSoftInputMode="stateHidden|adjustResize">

        </activity>
        <activity android:name=".VendorsActivity">

        </activity>
        <activity
            android:name=".VendorDetailsAvtivity"
            android:theme="@style/AppTheme.NoActionBar2">

        </activity>
    </application>

</manifest>